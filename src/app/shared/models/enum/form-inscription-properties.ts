export enum InscriptionFormProperties {
    TYPE_USER = "type_user",
    TYPE_COOP = "type_cooperative",
    NOM = "nom",
    EMAIL = "email",
    PASSWORD = "password",
    CONFIRMPASSWORD = "confirm_password"
}

// groupe de cst pr éviter erreurs
// éviter if user === ""
// "type_user"= valeur reelle (remplace le TYPE_USER)